class SwipeDto {
  final String toHorseId;
  final String fromHorseId;
  final String decision;

  const SwipeDto({
    required this.toHorseId,
    required this.fromHorseId,
    required this.decision,
  });
}
