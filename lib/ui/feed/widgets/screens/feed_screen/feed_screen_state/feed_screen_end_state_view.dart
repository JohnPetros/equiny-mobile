import 'package:flutter/material.dart';

class FeedScreenEndStateView extends StatelessWidget {
  const FeedScreenEndStateView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Voce chegou ao fim da feed.'));
  }
}
